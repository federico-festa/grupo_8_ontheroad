<!DOCTYPE html>
<html lang="en">
<base href="/" />
<%- include('partials/head', {title: 'Registrate' , css: 'register.css' }) %>

    <body>
        <main>
            <%- include('./partials/header') %>
                <section class="account-page">
                    <h3>¡Creá tu cuenta!</h3>
                    <div class="container">
                        <form id="register-form" action="/user/register" method="POST" class="form">
                            <div class="form-group">
                                <label class="form-label " for="firstName">Nombre</label>
                                <input type="text" class="form-control" id="firstName" autocorrect="off" autocapitalize="off"
                                    name="firstName" placeholder="ej: Pablo" value="<%= locals.oldData ? oldData.firstName : null %>">
                                <div class="errors-msg">
                                    <div class="errors"></div>
                                    <% if(locals.errors && errors.firstName) { %>
                                        <p><%= errors.firstName.msg %> </p>
                                            <% } %>
                                </div>
                            </div>
                            <div class="form-group">
                                <label class="form-label " for="lastName">Apellido</label>
                                <input type="text" class="form-control" id="lastName" autocorrect="off" autocapitalize="off"
                                    name="lastName" placeholder="ej: Pérez" value="<%= locals.oldData ? oldData.lastName : null %>">
                                    <div class="errors-msg">
                                        <div class="errors2"></div>
                                        <% if(locals.errors && errors.lastName) { %>
                                            <p><%= errors.lastName.msg %> </p>
                                                <% } %>
                                    </div>
                            </div>
                            <div class="form-group">
                                <label class="form-label " for="email">Email</label>
                                <input type="text" class="form-control" id="email" autocorrect="off" autocapitalize="off"
                                    name="email" placeholder="ej: name@email.com" value="<%= locals.oldData ? oldData.email : null %>">
                                    <div class="errors-msg">
                                        <div class="errors3"></div>
                                        <% if(locals.errors && errors.email) { %>
                                            <p><%= errors.email.msg %> </p>
                                                <% } %>
                                    </div>
                            </div>
                            <div class="form-group">
                                <label class="form-label" for="password">Contraseña</label>
                                <input type="password" class="form-control" id="password" autocorrect="off" autocapitalize="off"
                                    autocomplete="off" name="password" placeholder="8 caratéres mínimo">
                                    <div class="errors-msg">
                                        <div class="errors4"></div>
                                        <% if(locals.errors && errors.password) { %>
                                           <p><%= errors.password.msg %> </p> 
                                                <% } %>
                                    </div>
                            </div>
                            <button class="register-button" type="submit">Crear cuenta</button>
                            <button class="reset-button" type="reset">Borrar todo</button>
                        </form>
                        <p class="text-center t2">¿Ya tenés una cuenta? <a href="/user/login" class="login-button">Iniciá
                                sesión</a>
                        </p>
                    </div>
                </section>
        </main>
        <script src="js/header.js"></script>
        <script src="js/register.js"></script>
        <%- include('./partials/footer') %>
    </body>

</html>
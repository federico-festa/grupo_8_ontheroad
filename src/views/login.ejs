<!DOCTYPE html>
<html lang="en">
<base href="/" />
<%- include('partials/head', {title: 'Ingresá' , css: 'login.css' }) %>


    <body>
        <%- include('./partials/header') %>
            <main>
                <section class="account-page">
                    <h3>¡Ingresá a tu cuenta!</h3>
                    <div class="container">
                        <form id="login-form" action="/user/login" method="POST" class="form">
                            <div class="form-group">
                                <label class="form-label " for="email">Email</label>
                                <input type="text" class="form-control" autocorrect="off" autocapitalize="off"
                                    name="email">
                                    <div class="errors-msg">
                                        <% if(locals.errors && errors.email) { %>
                                            <p><%= errors.email.msg %> </p>
                                                <% } %>
                                    </div>
                            </div>
                            <div class="form-group ">
                                <label class="form-label" for="password">Contraseña</label>
                                <input type="password" class="form-control" autocorrect="off" autocapitalize="off"
                                    autocomplete="off" name="password">
                                    <div class="errors-msg">
                                        <% if(locals.errors && errors.password) { %>
                                            <p><%= errors.password.msg %> </p>
                                        </div>
                                            <div class="text-center">
                                                <a href="#account/reset" class="forgot-button">¿Olvidaste tu contraseña?</a>
                                            </div>
                                                <% } %>
                            </div>
                            <input type="checkbox" class="checkbox" name="rememberUser"> Recordar mi usuario
                            <button class="sesion-button" type="submit">Iniciar sesión</button>
                        </form>
                        <p class="text-center t2">¿No tenés una cuenta? <a href="/user/register"
                                class="register-button">Registráte</a></p>
                    </div>
                </section>
            </main>
            <%- include('./partials/footer') %>
    </body>

</html>